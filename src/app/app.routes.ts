import { Routes } from '@angular/router';
import { OpencloseComponent } from './openclose/openclose.component';



export const routes: Routes = [
    {path:'',component: OpencloseComponent, title:'page de transition' }
];
